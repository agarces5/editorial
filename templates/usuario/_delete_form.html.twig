<form method="post" action="{{ path('app_usuario_delete', {'nickname': usuario.nickname}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario.uuid) }}">
    <button class="btn">Delete</button>
</form>
